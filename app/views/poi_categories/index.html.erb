<h1>Listing poi_categories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @poi_categories.each do |poi_category| %>
  <tr>
    <td><%= link_to 'Show', poi_category %></td>
    <td><%= link_to 'Edit', edit_poi_category_path(poi_category) %></td>
    <td><%= link_to 'Destroy', poi_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Poi category', new_poi_category_path %>
