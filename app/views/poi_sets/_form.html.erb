<%= form_for(@poi_set) do |f| %>
  <% if @poi_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poi_set.errors.count, "error") %> prohibited this poi_set from being saved:</h2>

      <ul>
      <% @poi_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
